name: e2e-tests
on: [push]
jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Movendo diretório
        run: cd /home/runner/work/cypress-agibank
      - name: Cypress run
        uses: cypress-io/github-action@v5